===== $(date -Iseconds) — last 1000 lines (via Supervisor API) =====
[33m2026-01-09 02:00:00.117 WARNING (MainThread) [homeassistant.helpers.template] Template variable warning: 'homeassistant.util.read_only_dict.ReadOnlyDict object' has no attribute 'min' when rendering '# Home Assistant Configuration
_Last Updated: {{ now().isoformat() }}_

## 🛠 UI Helpers
Below are the input helpers configured in Home Assistant.

{% set helpers = ['input_boolean', 'input_button', 'input_datetime', 'input_number', 'input_select', 'input_text', 'counter', 'timer', 'schedule'] %}
{% for domain in helpers %}
{% set assets = states[domain] | list %}
{% if assets | count > 0 %}
### {{ domain | replace('input_', '') | capitalize }}
| Entity ID | Name | State | Attributes |
|---|---|---|---|
{% for s in assets | sort(attribute='entity_id') %}
{% set details = [] %}
{% if domain == 'input_number' %}
  {% set details = details + ['min=' ~ s.attributes.min, 'max=' ~ s.attributes.max, 'step=' ~ s.attributes.step] %}
{% elif domain == 'input_select' %}
  {% set details = details + ['options=' ~ s.attributes.options | join(', ')] %}
{% elif domain == 'input_datetime' %}
  {% set details = details + ['has_date=' ~ s.attributes.has_date, 'has_time=' ~ s.attributes.has_time] %}
{% elif domain == 'timer' %}
  {% set details = details + ['duration=' ~ s.attributes.duration] %}
{% elif domain == 'counter' %}
  {% set details = details + ['min=' ~ s.attributes.min, 'max=' ~ s.attributes.max] %}
{% endif %}

| `{{ s.entity_id }}` | **{{ s.name | replace("'", "'\\''") }}** | {{ s.state | replace("'", "'\\''") }} | {{ details | join(', ') | replace("'", "'\\''") }} |
{% endfor %}

{% endif %}
{% endfor %}

---

## 🏠 All Entities (By Domain)
{% for domain, items in states | groupby('domain') %}
{% if domain not in helpers and domain != 'group' %}
<details><summary><b>{{ domain | title }} ({{ items | length }})</b></summary>

| Entity ID | Name | State |
|---|---|---|
{% for s in items | sort(attribute='entity_id') %}
| `{{ s.entity_id }}` | {{ s.name | replace("'", "'\\''") }} | {{ s.state | replace("'", "'\\''") }} |
{% endfor %}
</details>

{% endif %}
{% endfor %}'[0m
[33m2026-01-09 02:00:00.118 WARNING (MainThread) [homeassistant.helpers.template] Template variable warning: 'homeassistant.util.read_only_dict.ReadOnlyDict object' has no attribute 'max' when rendering '# Home Assistant Configuration
_Last Updated: {{ now().isoformat() }}_

## 🛠 UI Helpers
Below are the input helpers configured in Home Assistant.

{% set helpers = ['input_boolean', 'input_button', 'input_datetime', 'input_number', 'input_select', 'input_text', 'counter', 'timer', 'schedule'] %}
{% for domain in helpers %}
{% set assets = states[domain] | list %}
{% if assets | count > 0 %}
### {{ domain | replace('input_', '') | capitalize }}
| Entity ID | Name | State | Attributes |
|---|---|---|---|
{% for s in assets | sort(attribute='entity_id') %}
{% set details = [] %}
{% if domain == 'input_number' %}
  {% set details = details + ['min=' ~ s.attributes.min, 'max=' ~ s.attributes.max, 'step=' ~ s.attributes.step] %}
{% elif domain == 'input_select' %}
  {% set details = details + ['options=' ~ s.attributes.options | join(', ')] %}
{% elif domain == 'input_datetime' %}
  {% set details = details + ['has_date=' ~ s.attributes.has_date, 'has_time=' ~ s.attributes.has_time] %}
{% elif domain == 'timer' %}
  {% set details = details + ['duration=' ~ s.attributes.duration] %}
{% elif domain == 'counter' %}
  {% set details = details + ['min=' ~ s.attributes.min, 'max=' ~ s.attributes.max] %}
{% endif %}

| `{{ s.entity_id }}` | **{{ s.name | replace("'", "'\\''") }}** | {{ s.state | replace("'", "'\\''") }} | {{ details | join(', ') | replace("'", "'\\''") }} |
{% endfor %}

{% endif %}
{% endfor %}

---

## 🏠 All Entities (By Domain)
{% for domain, items in states | groupby('domain') %}
{% if domain not in helpers and domain != 'group' %}
<details><summary><b>{{ domain | title }} ({{ items | length }})</b></summary>

| Entity ID | Name | State |
|---|---|---|
{% for s in items | sort(attribute='entity_id') %}
| `{{ s.entity_id }}` | {{ s.name | replace("'", "'\\''") }} | {{ s.state | replace("'", "'\\''") }} |
{% endfor %}
</details>

{% endif %}
{% endfor %}'[0m
[33m2026-01-09 02:00:00.193 WARNING (Recorder) [homeassistant.components.recorder.db_schema] Event data for call_service exceed maximum size of 32768 bytes. This can cause database performance issues; Event data will not be stored[0m
[32m2026-01-09 02:00:01.454 INFO (MainThread) [homeassistant.components.script.create_push_ha_info_to_github_newest] Push Ha Info to GitHub & Create AI Zip File: Executing step call service[0m
