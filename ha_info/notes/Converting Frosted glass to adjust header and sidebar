Frosted Glass Shell — Changes From Official Theme

CHANGE

Line 4

Change:

Frosted Glass:

to:

Frosted Glass Shell New:

⸻

DELETE

Light mode card-mod theme

Delete line 7:

      card-mod-theme: "Frosted Glass Light"

Light mode old sidebar

Delete lines 296–302:

        /* SIDEBAR BLUR */
        .mdc-drawer .mdc-drawer__content {
          backdrop-filter: blur(6px) saturate(1.2) !important;
          -webkit-backdrop-filter: blur(6px) saturate(1.2) !important;
          background: rgba(234, 235, 238, 0.7) !important;
          box-shadow: var(--ha-card-glass-inset-shadow) !important;
        }

Light mode old drawer

Delete lines 314–317:

        /* DRAWER SCRIM */
        ha-drawer {
          --mdc-drawer-scrim-color: rgba(0, 0, 0, 0.6) !important;
        }

Dark mode card-mod theme

Delete line 508:

      card-mod-theme: 'Frosted Glass Dark'

Dark mode old sidebar

Delete lines 782–788:

        /* SIDEBAR BLUR */
        .mdc-drawer .mdc-drawer__content {
          backdrop-filter: blur(8px) saturate(1.1) !important;
          -webkit-backdrop-filter: blur(8px) saturate(1.1) !important;
          background: rgba(25, 28, 45, 0.8) !important;
          box-shadow: var(--ha-card-glass-inset-shadow) !important;
        }

Dark mode old drawer

Delete lines 800–803:

        /* DRAWER SCRIM */
        ha-drawer {
          --mdc-drawer-scrim-color: rgba(0, 0, 0, 0.8) !important;
        }

⸻

ADD

Add this directly under Frosted Glass Shell New::

  card-mod-theme: 'Frosted Glass Shell New'
  card-mod-sidebar: |
    :host {
      --sidebar-background-color: rgba(30, 30, 30, 0.8) !important;
      --sidebar-icon-color: rgba(234, 235, 238, 0.6) !important;
      --sidebar-text-color: rgba(234, 235, 238, 0.8) !important;
      --sidebar-selected-icon-color: rgba(234, 235, 238, 0.95) !important;
      --sidebar-selected-text-color: rgba(234, 235, 238, 0.98) !important;
      --sidebar-menu-button-text-color: rgba(234, 235, 238, 0.98) !important;
      background: rgba(25, 28, 45, 0.8) !important;
      backdrop-filter: blur(8px) saturate(1.1) !important;
      -webkit-backdrop-filter: blur(8px) saturate(1.1) !important;
      box-shadow:
        3px 3px 0.5px -3.5px rgba(255, 255, 255, 0.15) inset,
        -2px -2px 0.5px -2px rgba(255, 255, 255, 0.10) inset,
        0 0 8px 1px rgba(255, 255, 255, 0.06) inset,
        0 0 2px 0 rgba(0, 0, 0, 0.18) !important;
    }
  card-mod-drawer: |
    wa-drawer::part(dialog) {
      background: transparent !important;
    }
    wa-drawer {
      --backdrop-filter: blur(8px) saturate(1.1);
    }
  card-mod-root: |
    .header {
      --app-header-text-color: rgba(240, 243, 255, 0.95) !important;
      background-color: rgba(30, 30, 30, 0.01) !important;
      color: var(--app-header-text-color) !important;
      backdrop-filter: blur(8px) saturate(1.1) !important;
      -webkit-backdrop-filter: blur(8px) saturate(1.1) !important;
    }
  card-mod-more-info-yaml: |
    ha-more-info-info$: |
      :host {
        color: var(--shell-more-info-primary) !important;
      }
    ha-more-info-history-and-logbook$: |
      :host {
        color: var(--shell-more-info-primary) !important;
        --primary-text-color: var(--shell-more-info-primary) !important;
        --secondary-text-color: var(--shell-more-info-secondary) !important;
      }
    ha-more-info-settings$:
      .: |
        :host {
          --ha-color-form-background: transparent !important;
          --ha-color-border-neutral-loud: var(--shell-more-info-border) !important;
          --ha-color-text-secondary: var(--shell-more-info-secondary) !important;
        }
      entity-registry-settings$:
        entity-registry-settings-editor$:
          ha-input.name$: |
            :host(:focus-within) wa-input::part(input) {
              color: var(--primary-color) !important;
            }
          ha-input.entityId$: |
            :host(:focus-within) wa-input::part(input) {
              color: var(--primary-color) !important;
            }

Add inside modes: light::

      shell-more-info-primary: 'rgba(19, 21, 54, 0.98)'
      shell-more-info-secondary: 'rgba(19, 21, 54, 0.8)'
      shell-more-info-border: 'rgba(19, 21, 54, 0.35)'

Add inside modes: dark::

      shell-more-info-primary: 'rgba(234, 235, 238, 0.98)'
      shell-more-info-secondary: 'rgba(234, 235, 238, 0.8)'
      shell-more-info-border: 'rgba(234, 235, 238, 0.45)'